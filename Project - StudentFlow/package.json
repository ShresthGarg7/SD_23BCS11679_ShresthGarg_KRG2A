{
  "name": "project---fs---springboot",
  "version": "1.0.0",
  "description": "StudyFlow is a production-ready, full-stack Kanban-style productivity application designed specifically for students. It features a modern React frontend and a robust Java Spring Boot backend with MongoDB.",
  "main": "index.js",
  "scripts": {
    "start": "docker compose up -d redis && concurrently \"npm run dev --prefix frontend\" \"mvn -f backend/pom.xml spring-boot:run\"",
    "stop": "docker compose down",
    "frontend": "npm run dev --prefix frontend",
    "backend": "mvn -f backend/pom.xml spring-boot:run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
