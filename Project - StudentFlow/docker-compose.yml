services:
  redis:
    image: redis:latest
    container_name: studyflow-redis
    ports:
      - "6379:6379"
    restart: always
    # Uncomment below for persistence if needed
    # volumes:
    #   - redis_data:/data

# volumes:
#   - redis_data:
